# 简历条目：ISP Atlas

> 直接复制使用；【】内按实际情况替换。文末附"进阶版"（完成训练/部署后替换用）。

---

## 版本一：完整版（适合项目经历栏，篇幅充足）

**ISP Atlas——图像信号处理可视化系统（传统 ISP 流水线 + 深度学习研究图鉴）**
个人项目 ｜ 技术栈：JavaScript（Canvas / TypedArray / SVG）· HTML5 · CSS3，零第三方依赖

**传统 ISP 方向：**
- 独立设计并实现 **12 级 RAW→RGB 全流程 ISP**：黑电平校正、坏点校正（ADC 上下溢出 rail 检测 + 邻域中值替换）、镜头阴影校正 LSC（渐晕逆增益补偿）、RAW 域中值降噪、自动白平衡（灰度世界 / 动态阈值 / 完美反射体 3 种算法）、去马赛克（双线性 / 边缘自适应 + 色差平滑）、双边滤波降噪、CCM 色彩校正、Gamma 编码、USM 锐化、色彩增强；每级支持独立开关、实时调参与前后对比滑块查看
- 实现**物理正确的传感器 RAW 模拟管线**：sRGB 线性化 → CFA 光谱串扰 → RGGB 马赛克采样 → 光源色温偏移 → 高斯读出噪声 → 坏点注入 → 镜头渐晕，使 AWB / CCM / LSC 各校正级均有真实作用对象，可复现"CCM 放大插值误差"等经典现象
- 全流程在**线性域以 Float32 传递**，8-bit 量化仅发生在 Gamma 编码之后，避免暗部信息被中间量化摧毁；CCM 取串扰矩阵的严格逆（数值验证 CCM·X_T = I）
- 手写 **PSNR 与 11×11 高斯窗 SSIM** 实时评估模块，支持逐级消融实验，定量分析各模块在"保真指标 ↔ 成像观感"之间的取舍
- 实现 **12 类经典算子交互 Demo**：直方图均衡 / CLAHE（tile + 裁剪限幅 + 双线性融合）、高斯 / 中值 / 双边 / 非局部均值滤波、Sobel / Laplacian、Canny（NMS + 双阈值滞后连接）、Otsu 自动阈值、形态学腐蚀膨胀开闭运算

**深度学习方向：**
- 构建 ISP / 图像修复**深度学习研究图鉴**：覆盖低照度 RAW 成像、图像降噪、联合去马赛克与降噪、All-in-One 盲修复、超分辨率、去模糊、端到端 / 可逆 ISP、低照度 sRGB 增强 **8 大领域 × 18 个代表模型**（SID、ELD、PMN、DnCNN、NAFNet、Restormer、DMCNN、AirNet、PromptIR、AdaIR、SwinIR、HAT、MIMO-UNet、Invertible-ISP、MAI Learned ISP、Zero-DCE、SCI、Retinexformer），每个模型含关键结构解读、数据集 / 损失函数 / 论文指标与开源仓库索引
- 自研**统一规范的 SVG 模型结构图渲染器**（支持纵向块图 + 跳连弧线、横向流程链两种布局），为 U-Net 跳连、NAFNet SimpleGate / SCA、Restormer 通道维转置注意力、可逆 ISP 仿射耦合块等关键结构绘制结构图并逐模块讲解
- 每个领域配套**"传统算法近似效果示意"**：用系统内置经典算法合成退化并恢复，建立"传统 ↔ 深度学习"对照学习视角

**工程与质量：**
- 纯原生 JavaScript 实现全部算法（约 **3400 行，0 运行时依赖**），Canvas 像素级实时计算；通过浏览器自动化逐页实测，**定位并修复 6 个算法级缺陷**（Bayer 统计采样相位错误、线性域中间量化损失、浮点值索引 TypedArray、参数 scale 级联等），核心质量指标从 PSNR 9.8dB 修复至 **22.9dB**（全退化开启）

---

## 版本二：精简版（每条一行，适合版面紧张）

**ISP Atlas——图像信号处理可视化系统**（JavaScript / Canvas / SVG，零依赖，约 3400 行）
- 独立实现 12 级 RAW→RGB ISP 流水线（BLC / 坏点 rail 检测 / LSC / AWB×3 / 边缘自适应去马赛克+色差平滑 / 双边降噪 / CCM 严格逆 / Gamma / USM），每级可开关调参、对比查看，PSNR/SSIM（手写高斯窗实现）实时评估
- 构建物理正确的传感器 RAW 模拟管线（光谱串扰 / 色温偏移 / 读出噪声 / 渐晕），全程线性域 Float32 传递，8-bit 量化仅存在于 Gamma 编码后
- 实现 12 类经典算子交互库（CLAHE / 双边 / NLM / Canny / Otsu / 形态学等）与 8 大领域 × 18 个深度学习模型研究图鉴，自研 SVG 结构图渲染器绘制 SID / NAFNet / Restormer / Invertible-ISP 等模型结构
- 浏览器自动化实测自审，修复 6 个算法级缺陷（采样相位 / 量化 / 浮点索引等），指标 9.8dB → 22.9dB

---

## 版本三：进阶版（完成"复现 + 训练 + 部署"后，替换或追加深度学习部分）

**深度学习方向（完成训练后使用）：**
- 复现 **SID（CVPR'18）** 于 SID Sony 子集，端到端 RAW 低照度成像达到论文水平 PSNR；在此基础上参考 PMN（TPAMI'23）引入噪声建模模块，【+0.xx dB】
- 复现 **NAFNet（ECCV'22）** 于 SIDD 真实噪声数据集至论文指标（PSNR 40.3），【替换 xx 模块 +0.2dB 并完成完整消融】
- 将模型导出 ONNX → **TensorRT INT8 量化部署**，推理加速【3.x×】，精度损失【0.0x dB】；量化后模型接入本可视化系统作为降噪级可选后端

---

## 面试防身要点（不写简历，但必须会讲）

1. **为什么坏点校正用 ADC rail 检测而不是"偏离 3×3 中值即判坏点"？** —— 后者会把高频纹理当坏点误杀（我在系统里实际复现过这个 bug）。
2. **为什么处理链要在线性域 Float32？** —— 中间 8-bit 量化会摧毁暗部；且 CCM、Gamma 数学上只在线性域成立。
3. **为什么开了 CCM/色彩增强后 PSNR 反而降？** —— CCM 会放大的插值误差（这正是 DMCNN 等联合去马赛克+降噪研究的动机）；色彩增强追求观感而非保真，真实手机 ISP 调校同样如此。
4. **NAFNet 为什么可以去掉激活函数？** —— SimpleGate（通道对半相乘）提供非线性，SCA 用逐通道全局注意力替代自注意力，同样算力下更快更准。
5. **Restormer 如何让 Transformer 处理高分辨率图？** —— MDTA 在通道维算注意力（C×C 协方差），复杂度随分辨率线性增长。
